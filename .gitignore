# dependencies
node_modules/

# build output (GitHub Actions builds & deploys; don't commit dist)
dist/

# local Claude Code settings (personal, machine-specific)
.claude/settings.local.json

# local layout preview (mirror of src/App.jsx, opened in a browser)
preview.html

# logs & editor cruft
*.log
.DS_Store
